# ─────────────────────────────────────────────────────────────────
# Streamlit secrets — TEMPLATE
# ─────────────────────────────────────────────────────────────────
# LOCAL USE:
#   1. Copy this file to  .streamlit/secrets.toml
#   2. Set your password below
#   3. .streamlit/secrets.toml is gitignored — it will NOT be committed
#
# STREAMLIT CLOUD:
#   Do NOT commit secrets.toml. Instead, paste the line below into the
#   app's "Secrets" panel (Manage app → Settings → Secrets).
# ─────────────────────────────────────────────────────────────────

app_password = "change-me-to-a-strong-password"
